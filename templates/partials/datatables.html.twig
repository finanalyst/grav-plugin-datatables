{{ content|raw }}

<script>
  $(document).ready( function () {
      $('#{{  id }}').DataTable({% if options %}JSON.parse("{{options|escape('js')}}"){% endif %});
      {% if snippet %}
        const selector = '#{{ id }}';
        {{ snippet }}
      {% endif %}
  } );
</script>
