{{ content }}
<script>
$(document).ready( function () {
    $('#{{  id }}').DataTable({% if options %}{{ options }}{% endif %});
    {% if snippet %}
      var selector = '#{{ id }}';
      {{ snippet }}
      {% endif %}
} );
</script>
